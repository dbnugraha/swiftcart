{
  "name": "@swiftcart/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Types and money helpers shared by the API and the mobile client.",
  "//": "Compiled to dist rather than shipping TypeScript source. A .ts entry point works for Metro but NodeNext resolution refuses to follow it, so the API could not typecheck against it. Building emits .js with explicit extensions plus .d.ts, which both resolvers accept.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
