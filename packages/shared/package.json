{
  "name": "@swiftcart/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Types and money helpers shared by the API and the mobile client.",
  "//": "Ships TypeScript source, consumed by tsx on the API side and Metro on the mobile side — neither needs a build step. `type: module` is required or Node resolves it as CommonJS and named imports fail.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
